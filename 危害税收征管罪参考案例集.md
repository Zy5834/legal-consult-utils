```
# 危害税收征管罪 · 人民法院案例库参考案例知识库
## 1. 数据源简介与选择理由
数据源为**人民法院案例库（rmfyalk.court.gov.cn，最高人民法院官方平台）**，选取刑法分则【危害税收征管罪】相关参考案例。本次初始采集共10篇案例文档。
选择理由：
1. 数据源权威，裁判文书、案情、裁判要点完整，来源可溯源；
2. 案例题材统一为危害税收征管罪，适合法律案例文本处理、知识库构建；
3. 案例页面支持网页提取，适配OpenCLI工具流水线；
4. 满足作业不少于10份文档的基础要求；
5. 仓库支持后续持续新增涉税案例，构建可迭代法律案例知识库。

> 浏览器说明：本项目使用 **Microsoft Edge（Chromium内核）**，安装OpenCLI Browser Bridge扩展，复用Edge浏览器登录会话抓取。

## 2. 数据采集方法说明
使用OpenCLI browser模式抓取，依托Microsoft Edge（Chromium内核）浏览器 + OpenCLI Browser Bridge 扩展（复用已登录案例库的统一账户会话）：
1. Edge浏览器访问人民法院案例库网页，扫码登录后页面保持打开（**必须已登录**，否则正文接口 401/跳 SSO 登录页）；
2. 在已登录浏览器内逐一对危害税收征管罪**6 个具体子罪名**（逃税罪、抗税罪、逃避追缴欠税罪、骗取出口退税罪、虚开增值税专用发票罪、虚开发票罪）发起全文检索；
3. 从检索结果 `.al-list` 卡片提取案例 id/标题/法院/程序，去重并按标题含"税/发票"筛选，得到唯一案例清单（约 17 篇）；
4. 通过浏览器内 `eval` 直接 `fetch` 正文接口 `POST /cpws_al_api/api/cpwsAl/content`（请求体 `{gid: encodeURIComponent(id)}`），取回完整字段（裁判要旨/基本案情/裁判理由/关联事由/裁判结果等），输出原始 Markdown 保存至 `00_raw`；
5. 单批抓取后预留间隔，降低网站风控风险；仅采集公开案例内容，本项目仅限课程作业学习使用，不商用、不对外分发。

> 注意：用户早期提供的 `value=刑事&lib=cpwsAl_02` 检索 URL 是「全部刑事参考案例」检索，结果混有大量非涉税案，不能作为本罪名数量依据；须按上述 6 个子罪名分别检索。

> 【新增案例采集统一规则】后续新增案例入库，必须沿用相同采集流程；原始文件存入`00_raw`，文件名统一格式 `case_序号_案例标题.md`；必须保留原始网页URL来源，不粘贴无来源文本。

## 3. 文本清洗流水线
清洗工具：sed + Python脚本
1. 清理网页冗余内容：导航栏、页脚、广告、多余空白换行；
2. 统一中英文标点，压缩多余空行；
3. 人工逐篇核验案情、裁判要点等核心内容，保证正文完整；
4. 清洗完成后进入脱敏处理环节。

> 【新增案例清洗规则】新增案例使用同一套清洗脚本，保证文本标准统一；清洗完成后人工复核，剔除网页残留垃圾文本。

## 4. 信息脱敏处理
目标：将案例内企业主体、自然人姓名做匿名脱敏；法院名称、法条原文保留不修改。
1. Python正则脚本自动替换：自然人姓名替换为【自然人A/B…】，企业名称替换为【主体A/B…】；
2. 脚本执行完成后人工复核全文；
3. 确保无遗漏真实人名、企业名称泄露。

> ⚠️ **脱敏默认约定（2026-09-10 修正）**：人民法院案例库、裁判文书网、北大法宝等**官方已公开发布且自带"某"化/匿名化**的文书，后续默认**不再做二次脱敏**——直接保留官方脱敏形态即可。本仓库当前 `01_clean/` 的 10 篇是应**特定作业要求**做的二次脱敏，属例外；后续新增案例入库时，`01_clean` 直接复用官方"某"化 + YAML + 纯文本，无需再跑脱敏脚本（除非再次明确要求二次脱敏）。

> 【新增案例脱敏规则】默认沿用官方脱敏形态（不二次脱敏）；若本次作业明确要求二次脱敏，则复用 `scripts/desensitize2.py`（精确字符串替换，非模糊正则）并人工复核，保证与存量 10 篇规则一致。

## 5. YAML Front Matter（YAML元数据，原YAML）规范
每一篇处理完成的md文档，**文档最顶部**添加元信息块，使用`---`包裹，用于rga检索、知识库管理。
固定字段：
- `title`：案例完整标题
- `source`：数据源（固定：人民法院案例库）
- `url`：案例原始网页链接
- `publish_date`：案例发布日期
- `category`：案例分类（固定：危害税收征管罪）
- `tags`：检索标签数组（偷税、虚开增值税专用发票等罪名标签）

> 【新增案例元数据规则】新增入库案例必须补齐完整YAML元信息，字段名称、格式和存量文档保持一致，保障全文检索统一。

## 6. Git版本管理与知识库迭代规则
> 仓库定位：可迭代法律案例知识库，支持后续不断新增涉税案例，版本留存原始文件与成品文件。
1. **分支规范**
    - `raw`分支：存放00_raw目录，全部**未清洗原始抓取md文件**，原始数据永久保留；
    - `clean`分支：存放`01_clean`目录，存放脱敏、清洗、带YAML元数据的成品文档，作业提交使用此分支；
    - 新增案例开发：新建`feature/add-case-xxx`分支，完成采集、清洗、脱敏、复核后，合并回raw与clean主分支。
2. **提交commit规范**
    - 原始案例入库：`feat: 导入原始案例：案例名称`
    - 成品文件处理：`feat: 完成案例清洗、脱敏、添加元数据：案例名称`
3. **README更新规则**
    - 每新增一批案例入库，同步更新本README，更新案例总数量，追加案例清单；
    - 流水线脚本规则若无必要不随意修改；如需修改脚本，提交记录写明变更理由。
4. **版本标签**
    批量案例入库完成后打tag，例如`v1.0`（初始10篇危害税收征管罪案例），后续新增案例迭代为v1.1、v1.2，方便回溯不同版本知识库。

## 仓库目录结构
```

├── 00_raw/          # OpenCLI 抓取原始 md 文件（原始网页内容，持续归档）
├── 01_clean/        # 清洗、脱敏、添加 YAML 元信息后的成品案例文档
├── scripts/         # Python 脚本：脱敏脚本(desensitize2.py)、批量插入 YAML 元信息脚本
└── README.md        # 项目说明文档，新增案例时同步更新

> 当前进度：`00_raw/` 存 10 篇原始抓取（官方"某"化，含 YAML+纯文本正文）；`01_clean/` 存 10 篇已二次脱敏成品（企业→【主体A/B…】、自然人→【自然人A/B…】，法院与法条保留）。

```

## 7. 检索使用方式
本地使用rga工具，在`01_clean`目录对案例全文检索；可基于这批涉税案例搭建本地知识库，快速检索罪名、裁判观点。

## 8. 版权与使用声明
案例来源：人民法院案例库公开案例。**本仓库全部内容仅用于课程作业、个人法律学习，仓库设置私有，禁止公开发布、二次分发、商业用途。后续新增入库案例同样遵守该约束。**
